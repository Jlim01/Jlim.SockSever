<UserControl x:Class="RestruantHost.Main.Ctrl.TableModuleGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestruantHost.Main.Ctrl"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid x:Name="TableGrid" Background="White">
        <Rectangle Stroke="#FFE1E1E1"/>
        <Grid x:Name="gridBodyBackboard" Margin="50" Background="#FFE1E1E1">
            <Grid x:Name="gridBody" Margin="1 1 0 0"/>
        </Grid>
        <Grid x:Name="quickTip" Style="{StaticResource Shadow}"
              HorizontalAlignment="Left"
              Width="74" Height="31" 
              VerticalAlignment="Top"
              Opacity="0.7">
            <Rectangle x:Name="quickTipRect" Fill="Black"
                       Stroke="{DynamicResource ControlBorderBrush}"
                       RadiusX="6" RadiusY="6"/>
            <TextBlock x:Name="quickTipText"
                       Text="1-1" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Foreground="White" 
                       Style="{StaticResource Shadow}"/>
        </Grid>

    </Grid>
</UserControl>
